# 《虎牌 / Hupai》规则书

本文档描述**当前 Flask 网页版**（`app.py` + `templates/play.html`）中的牌张、流程与判定，便于线下改规则后再同步代码。实现与线下约定冲突时，以你为准改本文档，再按文档改算法。

**对应实现**：`app.py`（截至 2026-05 仓库现状）。

---

## 0. 项目是什么

- **类型**：四人地方牌类（虎牌）的 **单机网页练习桌**；你固定坐 **玩家 1**（下标 0），其余三家为人机。
- **技术**：Python **Flask** + 浏览器 **Session** 存局；`GET /start` 开局，`GET/POST /play` 对局。
- **界面**：绿绒四人桌；手牌、河牌、明面用 `static/切分/` 下的扫描图（`tile_image_url` 过滤器）；胡牌后弹层展示结算，分数写入 `scores`。
- **范围**：实现 **行牌、摸打、吃碰开、磕拢、自摸/荣胡** 与胡牌形 DFS；**无**杠、无荒牌流局、无一炮多响、无终局赛制（可 `/start` 重开）。

---

## 1. 游戏形式与角色

| 项 | 实现 |
|----|------|
| 人数 | 固定 **4**（`num_players = 4`） |
| 真人 | 下标 **0**（`human_player_index`） |
| 人机 | 每次 **GET `/play`** 由 `_advance_ai_until_human_turn` 推进：清空 AI 询价 → 非真人回合自动摸牌 → 随机出牌 |
| 胜负 | 自摸或荣胡时写入 `game_result` 并 **累加** `scores[0]`；页面弹层显示；**无**流局、无自动下一局（除访问 `/start`） |

---

## 2. 牌张构成（牌墙）

### 2.1 普通牌（每种 4 张）

数牌三类，每类 1～9，共 **27** 种 × 4 = **108** 张：

- **万子**：一万 … 九万  
- **筒子**：一筒 … 九筒  
- **条子**：一条 … 九条（内部牌名用「条」；扫描图目录名为「索」）

**杂牌**（各 4 张）：乌龟、毛、千万  

### 2.2 督牌（每种整局 1 张）

共 **5** 张：`万督`、`筒督`、`条督`、`妖督`、`总督`  

（资源文件名：`幺督.jpeg` → 牌名 **妖督**；`索督.jpeg` → **条督**；`总.jpeg` → **总督**。）

### 2.3 洗牌

`BASE_TILES * 4 + DU_TILES_UNIQUE`，`random.shuffle` → `draw_pile`（合计 **125** 张）。

### 2.4 牌名归一化

`_normalize_tile_name`：`索`→`条`，`幺督`→`妖督`。手牌计数、胡牌、河牌匹配均走归一化。

---

## 3. 开局（`/start`）

1. 新建牌墙并洗牌。  
2. **庄家** `zhuang = 0`（玩家 1）。  
3. 按 0→1→2→3 循环 `pop` 发牌：庄家 **17** 张，其余各 **16** 张。  
4. `current_player = 0`，`phase = "turn"`。  
5. 庄家起手视为已摸牌：`has_drawn_wall_this_turn = True`（第一手可直接出）。  
6. `dealer_opening_done = False`（庄家第一手出牌后置 `True`）。  
7. `discarded = []`，`melds` 四家空，`scores = [0,0,0,0]`，清除 `game_result` / `claim_state`。

---

## 4. Session 核心字段

| 字段 | 含义 |
|------|------|
| `hands` | 四家手牌 |
| `draw_pile` | 牌墙（`pop()` 摸牌） |
| `discarded` | 河牌 `(出牌者下标, 牌面)` |
| `melds[i]` | 玩家 i 明面，tuple 见第 8 节 |
| `scores[i]` | 累计分：碰 +10、拢 +70、**开** 再加一倍该组分、胡牌结算 +100+明面分 |
| `current_player` | 当前行牌权 |
| `phase` | `"turn"` / `"claim"`（询价） |
| `zhuang` | 庄家下标 |
| `dealer_opening_done` | 庄家是否已出第一手 |
| `has_drawn_wall_this_turn` | 本回合是否已从牌墙摸牌 |
| `last_wall_draw_tile` | 本回合最后摸上的牌（展示用） |
| `claim_state` | 询价：`discarder`, `tile`, `queue`, `idx` |
| `human_player_index` | 真人下标（0） |
| `game_result` | 最近一次胡牌结算（弹层用） |

---

## 5. 正常行牌（`phase == "turn"`）

### 5.1 须先摸牌（庄家第一手除外）

`has_drawn_wall_this_turn == False` 时须先摸牌（POST `action=draw_wall` 或 AI 自动摸）。摸牌后设 `has_drawn_wall_this_turn = True`，记录 `last_wall_draw_tile`。

### 5.2 牌墙为空

多处逻辑会 `new_shuffled_draw_pile()` **整副重洗替换**（无荒牌流局）。

### 5.3 出牌

轮到本人、已摸牌、无未完成询价；POST `tile=…` 从手牌移除并 `_finish_discard`。

---

## 6. 出牌、河牌与询价

### 6.1 进河

`discarded.append((D, tile))`；庄家第一手后 `dealer_opening_done = True`。

### 6.2 督牌打出：不询价

牌名 **含** `"督"` → 不建 `claim_state`；`current_player = (D+1)%4`，下家 `has_drawn_wall_this_turn = False`，`phase = "turn"`。他人不能吃碰胡开此张。

### 6.3 非督牌：进入询价

`claim_state = { discarder, tile, queue: [下家, 对家, 上家], idx: 0 }`，`phase = "claim"`。

### 6.4 真人询价 POST

轮到 `queue[idx] == human_ix` 时：

| action | 说明 |
|--------|------|
| `rong_hu` | 荣胡（`action=hu` 会映射为此） |
| `chi_claim` + `chi_tile_a/b` | 吃（仅上家） |
| `pong_claim` + 可选 `pong_substitute` | 碰（可一张同点 + 一张督） |
| `kai_claim` | **开**（见 7.4） |
| `pong_pass` | 过 |

错误用 `flash` + 重定向 `/play`，避免空白 400 页。

### 6.5 人机询价（随机）

`_auto_resolve_claim_queue_until_human`（轮到真人则停）：

1. 督牌河张 → 全员过。  
2. **开**：暗磕/暗拢与河牌同点，约 **10%** → `_execute_kai`。  
3. **碰**：`_can_pong` 为真，约 **12%** → `_execute_pong`。  
4. **吃**：上家且 `_chi_options` 非空，约 **11%** → 取**第一个**吃项。  
5. 否则 **过**（`idx += 1`）；三家过完 → `_finish_all_claims_passed`（行牌权给出牌者下家，须再摸牌）。

---

## 7. 吃、碰、开、荣胡（执行细节）

### 7.1 吃

- **仅上家**；河牌须为 **万/筒/条** 数牌。  
- 手牌两张与河牌成顺；缺张可用 **总督**（任意缺张）或 **妖督**（仅代该顺中的 **「一」** 点，即一万/一筒/一条位）。  
- 明面 `(a,b,c,"chi")`；吃后行牌权归吃家，`has_drawn_wall_this_turn = True`（不另摸牌）。

### 7.2 碰

- 河牌 1 + 手牌 2 张同点；或手牌 **1 张同点 + 1 张可用督**（`_valid_substitute_for_tile`）：  
  - **总督**：任意牌；  
  - **妖督**：杂牌或 **一万/一筒/一条**；  
  - **万督/筒督/条督**：同花色数牌。  
- 明面 `(tile, substitute或None, "pong")`；**+10** 入 `scores`；碰家直接出牌（不摸牌）。

### 7.3 荣胡（`evaluate_rong_hand` / `can_ronghu`）

- **时机**：询价阶段，他人刚打出的河牌（督牌打出无询价，不能荣胡）。  
- **判定**：手牌 + 明面 + 河牌 **+1 张**，与自摸相同，只问能否拆成 **一对将 + 若干顺子**（见第 9 节；督百搭仍生效）。  
- **结算**：河牌移除；`100 + 明面分` → `game_result`（荣胡、点炮者）。

### 7.4 开（`kai_claim`）

- 条件：本家明面存在 **暗磕** `(tile, None)` / `(tile, 督)` 或 **暗拢** `(tile, None, "long")`，且与河牌 **同点** `tile`。  
- 效果：翻为 **明磕** `(tile, sub, "ke_open")` 或 **明拢** `(tile, None, "long_open")`；**立即再加一倍该组分**（暗磕 +20、暗拢 +70）；从牌墙 **再摸一张**；行牌权归开牌者，须出牌。  
- 河牌该张**不**收入明面（与碰不同，仅“亮倒”已有组合）。

---

## 8. 明面（`melds`）编码

| 类型 | Tuple | 胡牌 multiset | 展示分 `_meld_item_score` |
|------|--------|----------------|---------------------------|
| 吃 | `(a,b,c,"chi")` len 4 | 各 +1 | 0 |
| 碰 | `(tile, sub, "pong")` len 3 | 同点 2 + 督 1 或 同点 3 | 10（**特殊牌 ×2 → 20**） |
| 拢（明） | `(tile, None, "long")` | +4 | 70（**特殊牌 ×2 → 140**） |
| 拢（开） | `(tile, None, "long_open")` | +4 | **140**（特殊牌 **280**） |
| 磕（暗） | `(tile, None)` 或 `(tile, 督)` len 2 | 同点 2+督1 或 +3 | 20（**特殊牌 ×2 → 40**） |
| 磕（开） | `(tile, sub, "ke_open")` | 同碰 | **40**（特殊牌 **80**） |

胡牌时 `_virtual_counter_for_win` 将明面折入 Counter；旧数据无 `"chi"` 标记的三张顺子仍按顺子 +1+1+1 兼容。

---

## 9. 胡牌判定（`evaluate_hand` / `evaluate_rong_hand`）

**唯一原则（自摸、荣胡共用）**：**手牌**（及桌上 **吃** 的顺子、荣胡时 +河牌 1 张）能否拆成 **恰好一个对子（将）+ 其余全部为顺子**（每组 3 张连续同花色；**禁止刻子**）。  
- 万/筒/条：只能顺子；  
- **妖牌**（乌龟、毛、千万）：仅 **乌龟→毛→千万** 这一顺；**妖督** 可代其中缺张（千万属妖，与 `_valid_substitute_for_tile` 一致）；  
- 桌上已 **碰 / 磕 / 拢** 的明面：**不参与** 拆牌（视为已定型面子）；**不因此不能胡**。  
- **手牌**里同点 **≥3 张**、尚未碰/磕/拢亮出：**不能胡**（须先亮出）。  
- 荣胡 = 河牌 +1 张，只与手牌 + **吃** 一并拆。  
除上述外，不再另设其它拦截。

### 9.1 将牌（`_try_remove_pair`）

- 任意牌 ≥2 张作将；  
- 万督+万子、筒督+筒子、条督+条子各 1 张作将；  
- 妖督 + **一万/一筒/一条** 或 + **乌龟/毛/千万** 作将；  
- 两张同花色督、两张妖督、两张总督作将；  
- 总督 + 任意另一张作将。

### 9.2 面子（无将部分）

- 万/筒/条：`万督`/`筒督`/`条督` 作对应花色 **百搭**；**妖督** 在该门仅作点数 **「一」** 的百搭（`yi_jokers`）；余牌由 `_suit_only_sequences` 只拆 **顺子**；  
- 妖牌：仅 **乌龟→毛→千万** 顺子；**妖督** 代缺张；不能剩杂牌或刻子；  
- **总督**：张数拆成三份加到万/筒/条的百搭额度（枚举 `_iter_nonneg_splits4` 中万/筒/条三分量），使三门均能用顺子拆完。

### 9.3 基础分

可胡则 `score = 100`（与番种无关）。

### 9.4 结算展示

自摸 / 荣胡均：`total = 100 + _meld_score_sum(melds)`，写入 `game_result` 并 `scores[human] += total`。荣胡记录 `win_tile`、`from_player`（点炮者座位号，1-based）。

---

## 9.1 特殊牌（碰 / 磕 / 拢得分 ×2）

下列牌面在 **碰、磕、拢**（含暗/明、胡牌结算里的明面分、**开** 时再加一倍基数）时，该组 **得分翻倍**（以明面主牌 `meld[0]` 为准，带督磕仍看所磕点数牌）：

**两筒、七万、八万、九条、千万**

| 操作 | 普通 | 特殊牌 |
|------|------|--------|
| 碰（立即 + 分） | 10 | **20** |
| 暗磕 | 20 | **40** |
| 明磕（开） | 40 | **80** |
| 暗拢 / 补拢、直接拢（立即加分） | 70 | **140** |
| 明拢（开） | 140 | **280** |

吃、荣胡/自摸基础分 **不受** 特殊牌影响。

---

## 10. 磕牌（POST `meld_tile`，可选 `substitute`）

- `phase=="turn"`、轮到本人、已摸牌、无询价。  
- **无刻督**：手牌同点 **≥3** → 明面 `(tile, None)`。  
- **带督**：手牌同点 **≥2** + 一张可用督 → `(tile, substitute)`；**总督可代任意牌**。  
- **≥3 张同点**：一律磕 **三张实牌**，不用督（即使手牌里还有总督、下拉框选了督也忽略）。  
- **仅 2 张同点**：才用督；表单未选督时自动选一张可用督（万/筒/条/妖督优先，其次总督）。  
- 磕 **不** 立即改 `scores`（开 / 胡时才体现明面分）。

---

## 11. 拢牌（POST `action=long`，`long_tile`）

### 11.1 补拢

明面为 **磕**（含带督）或 **碰**，且尚未是 `long` / `long_open`；手牌再出一张 **同点** 或 **可用督**（与磕/碰相同：杂牌/乌龟/毛/千万 可用 **妖督** 或 **总督**；数牌可用花色督或妖督代「一」）→ 改为 `(tile, None, "long")`，**+70**（特殊牌 **+140**），牌墙非空则 **再摸一张**。

### 11.2 直接拢

手牌同点 **≥4** → 移除四张，明面 `(tile, None, "long")`，**+70**（特殊牌 **+140**），再摸一张。

---

## 12. 界面与静态资源

- 路由：`/` 未单独列出时以 `/start` → `/play` 为主流程。  
- 牌图：`tile_scan_static_path` → `static/切分/{万|筒|索|杂|督}/….jpeg`；条子图文件名为 `{点数}索.jpeg`。  
- 手牌排序：万 → 筒 → 条 → 杂 → 督；**千万** 归入杂牌（不误判为万子）。  
- 询价时高亮 `claim_state.queue[idx]` 座位；展示可吃选项、可碰督列表、是否可开。

---

## 13. 与常见麻将的差异（便于改规则）

| 项 | 本实现 |
|----|--------|
| 荒牌 | 无；牌墙耗尽则重洗整副 |
| 一炮多响 | 无；询价顺序单家响应 |
| 杠 | 无；拢为四张明面，非杠包 |
| 吃碰优先级 | 人机随机；真人手动点选 |
| 督打出 | 不询价；手中督仍参与胡牌百搭 |
| 计分 | 以 `_meld_score_sum` + 胡牌 100 为主；碰/拢/开另有即时 `scores` 增量 |

---

## 14. 修订记录

| 日期 | 说明 |
|------|------|
| 初版 | 对应早期 `app.py`：胡牌 DFS、督禁询价、拢补花色督等 |
| 2026-05 | 对齐现行代码：牌墙每种 **4** 张；**开**；碰可用督；妖督吃牌仅代「一」；补拢含碰/带督磕；统一 `_meld_score_sum`（含 `ke_open`/`long_open`）；胡牌 `game_result` 与分数累加；切分扫描图；POST 错误 flash |

增删规则时，建议注明期望改动的函数（如 `_finish_discard`、`evaluate_hand`、`_execute_kai`），便于下一轮改代码。
