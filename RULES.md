# 《虎牌 / Hupai》规则书（对应实现：`app.py`，截至文档撰写时的代码行为）

本文档描述**当前 Flask 网页版实现**中的牌张构成、流程与判定算法，便于你在纸上扩展规则后再同步改代码。若实现与线下约定不一致，以你为权威更新本文档，再以文档为准改算法。

---

## 1. 游戏形式与角色

- **人数**：固定 **4** 人（代码里 `num_players = 4`，未支持可变人数）。
- **真人**：固定为 **玩家下标 0**（`human_player_index = 0`）。
- **其余三家**：人机；在每次 **GET `/play`** 时由 `_advance_ai_until_human_turn` 自动推进（摸牌、随机出牌、处理询价队列直到轮到真人或等待真人询价）。
- **胜负**：实现为 **胡牌时返回一段文本**（自摸胡 / 荣胡），**无**终局积分胜负判定、无流局、无一局结束后的重置（除访问 `/start` 重新开局）。

---

## 2. 牌张构成（牌墙）

### 2.1 普通牌（每种 4 张）

数牌三类，每类 1～9 点，共 27 种 × 4 = **108** 张：

- **万子**：一万 … 九万  
- **筒子**：一筒 … 九筒  
- **条子**：一条 … 九条  

**杂牌**（各 4 张）：

- 乌龟、毛、千万  

### 2.2 督牌（每种整局仅 1 张）

共 **5** 张，整副牌合计：

- 万督、筒督、条督、妖督、总督  

### 2.3 洗牌

- `BASE_TILES * 4` 再拼接 `DU_TILES_UNIQUE`，`random.shuffle` 得到牌墙 `draw_pile`。

---

## 3. 开局（`/start`）

1. 新建牌墙并洗牌。  
2. **庄家** `zhuang = 0`（玩家 1，下标 0）。  
3. 发牌顺序：按玩家下标 0→1→2→3 循环从牌墙 **pop**，每人抓满为止：  
   - 庄家 **17** 张；  
   - 其余三家各 **16** 张。  
4. `current_player = 0`（从庄家开始行牌）。  
5. `phase = "turn"`。  
6. **庄家第一手出牌前不必从牌墙再摸牌**：`has_drawn_wall_this_turn = True`。  
7. `dealer_opening_done = False`：标记庄家尚未完成“起手后的第一手出牌”（见 6.3）。  
8. `discarded = []`，各家 `melds = []`，`scores = [0,0,0,0]`。

---

## 4. 数据结构与界面无关的状态量

下列为服务端 `session` 中核心字段（玩法逻辑依赖它们，而不是 HTML）：

| 字段 | 含义 |
|------|------|
| `hands` | 四家手牌列表。 |
| `draw_pile` | 牌墙（列表尾部为栈顶，摸牌用 `pop()`）。 |
| `discarded` | 河牌列表，元素为 `(出牌者下标 D, 牌面字符串 tile)`。同一牌面可多次出现（不同打出者或不同时间）。 |
| `melds[i]` | 玩家 i 的明面组合列表， tuple 格式见第 9 节。 |
| `scores[i]` | 玩家 i **已结算的立即加分**（当前实现里：**碰 +10**、**拢 +70** 会写入；自摸/荣胡的展示分 mostly 为另一条计算路径）。 |
| `current_player` | 当前“行牌权”所在玩家下标。 |
| `phase` | `"turn"`：正常行牌；`"claim"`：有人出牌后在询价吃碰胡（见第 7 节）。 |
| `zhuang` | 庄家下标。 |
| `dealer_opening_done` | 庄家是否已完成起手后的第一手出牌（用于标记）。 |
| `has_drawn_wall_this_turn` | 当前玩家在本回合是否已从牌墙摸过牌（见第 5 节）。 |
| `claim_state` | 询价状态（见第 7 节）。 |
| `human_player_index` | 真人玩家下标（0）。 |

---

## 5. 正常行牌阶段（`phase == "turn"`）

### 5.1 回合内必须先摸牌（庄家第一手除外）

- 当轮到某玩家且 `has_drawn_wall_this_turn == False` 时，**必须先**从牌墙摸一张牌，才能出牌或做其他需要“已摸牌”的操作（磕、拢、自摸胡等）。  
- 真人通过 POST `action=draw_wall` 摸牌；人机在 `_advance_ai_until_human_turn` 里自动摸牌。  
- 摸牌后：`has_drawn_wall_this_turn = True`，并记录 `last_wall_draw_tile`（最后摸上的那张，便于 UI 展示）。

### 5.2 牌墙为空时的行为（实现细节）

- 多处逻辑若发现 `draw_pile` 为空，会调用 `new_shuffled_draw_pile()` **重新生成并洗牌一整副牌**替换牌墙（即：**牌墙永不枯竭**，与实体麻将“荒牌流局”不同）。

### 5.3 出牌

- 仅当 `current_player == 自己` 且 `has_drawn_wall_this_turn == True`（已摸牌）时允许出牌（庄家第一手因已设为已摸牌，可直接出）。  
- 出牌 POST 带 `tile=某张牌`，该牌必须从手牌中移除。  
- 调用 `_finish_discard(session, D, tile)` 将 `(D, tile)` 追加进 `discarded`。

---

## 6. 出牌后进河与“庄家起手标记”

### 6.1 进河

- `discarded.append((D, tile))`。

### 6.2 庄家起手

- 若出牌者为庄家且 `dealer_opening_done == False`，则在本笔弃牌后将其置为 `True`（表示庄家已完成起手后第一手出牌）。

---

## 7. 询价阶段（`phase == "claim"`）——吃、碰、荣胡

### 7.1 何时进入询价

- `_finish_discard` 在进河后，若 **打出牌不是督牌**（见第 8 节），则创建：  
  - `claim_state = { "discarder": D, "tile": tile, "queue": [(D+1)%4,(D+2)%4,(D+3)%4], "idx": 0 }`  
  - `phase = "claim"`  
- 即：**下家 → 对家 → 上家** 依次决策（注意：这是**座位顺序**，不是“优先级大小”，实现里也未做抢杠胡等）。

### 7.2 督牌打出：完全不询价

- 若 `_is_du_tile(tile)` 为真（牌名字符串 **包含** `"督"` 即视为督牌，含万督/筒督/条督/妖督/总督）：  
  - **不创建** `claim_state`；  
  - 直接 `current_player = (D + 1) % 4`；  
  - `has_drawn_wall_this_turn = False`（下家须先摸牌）；  
  - `phase = "turn"`。  
- **推论**：他人 **不能吃、碰、荣胡** 这张河牌（因为没有进入询价；真人若误 POST，服务端也会清理到Pass）。

### 7.3 真人询价时的可提交动作（POST）

仅当 `claim_state` 存在且 `queue[idx] == human_ix`：

- **荣胡** `action=rong_hu`：见第 10.3 节。  
- **吃** `action=chi_claim`，并提交 `chi_tile_a`、`chi_tile_b`：见第 9.3 节。  
- **碰** `action=pong_claim`：见第 9.2 节。  
- **过** `action=pong_pass`：`idx += 1`；若越界则 `_finish_all_claims_passed`（见 7.5）。

### 7.4 人机在询价中的自动策略（随机）

`_auto_resolve_claim_queue_until_human`（仅供 AI）：

- 若当前询价牌为督牌：直接视作全员通过（`_finish_all_claims_passed`）。  
- 否则对当前 `resp`：  
  - 若轮到真人：停止，等待真人。  
  - 否则：  
    - 若手牌中 `tile` **不少于 2 张**，以 **12%** 概率选择 **碰**（`_execute_pong`）。  
    - 否则若该玩家 **可吃**（是上家）且 `_chi_options` 非空，以 **11%** 概率吃（取 **第一个** 吃选项）。  
    - 否则 **过**（`idx += 1`）。  
- 若连续既不能吃又不能碰：也会 **过**（提前推进 `idx`）。

### 7.5 三家全部“过”之后

`_finish_all_claims_passed`：

- 清空 `claim_state`；  
- `current_player = (D + 1) % 4`（出牌者的下家）；  
- `has_drawn_wall_this_turn = False`（下家到达后须先摸牌）；  
- `phase = "turn"`。

---

## 8. 明面组合（`melds`）的编码与含义

实现用 tuple 表示，长度与末位标记决定类型：

| 形状 | Tuple 形式 | 备注 |
|------|----------------|------|
| **吃** | `(a, b, c, "chi")`，长度 4 | a,b,c 为顺子三张（已排序关键序）。 |
| **碰** | `(tile, None, "pong")`，长度 3 | 三张相同。 |
| **拢** | `(tile, None, "long")`，长度 3 | 四张相同（明面展示为拢）。 |
| **磕（无明督）** | `(tile, None)`，长度 2 | 三张相同刻子。 |
| **磕（带督代）** | `(tile, substitute)`，长度 2 | 两张 `tile` + 一张督 `substitute`。 |

胡牌判定时，上述明面会按 `_virtual_counter_for_win` 折叠进 multiset（见第 11 节）。

---

## 9. 吃、碰、执行的算法细节

### 9.1 谁可以吃

- **只有上家**：解析玩家索引，`human_ix` 为当前响应者时，要求 `(human_ix + n - 1) % n == discarder`。  
- 若弃牌不是 **万/筒/条数牌**（例如杂牌、督牌），则不能吃（`_parse_suit_rank_tile` 无法解析 → 无吃）。

### 9.2 碰（`_execute_pong`）

- 条件：响应者手牌中 `tile` **至少 2 张**（加上河牌共三张同）。  
- 从河中 **移除最近一笔** 匹配的 `(D, tile)`（自后向前扫描）。  
- 手牌移除两张该牌；明面追加 `(tile, None, "pong")`。  
- **立即加分**：`scores[responder] += 10`。  
- 行牌权：`current_player = responder`；`has_drawn_wall_this_turn = True`（**碰后不摸牌，直接出牌**）；`phase = "turn"`。

### 9.3 吃（`_execute_chi`）

- 前置：`responder` 必须是弃牌者的上家；且 `_chi_triplet_valid(discard, h1, h2)` 为真。  
- **顺子范围**：仅 **万/筒/条** 数牌，顺子跨越九点不循环（起点 `start ∈ [0,6]` 对应三点连续 rank）。  
- **手中两张**：可以是两张具体数牌，也可以其中一张或两张为 **总督**（`总督`）代替缺张（算法与 `_chi_options` 一致）：  
  - `_chi_options` 枚举：对固定的顺子，手牌可提供 `(t1,t2)`、`(t1,总督)`、`(t2,总督)`、`(总督,总督)` 等组合（在手牌张数足够时）。  
- 河牌移除同上；手牌移除 `h1`、`h2`（若两点相同需足够张数）。  
- 明面记录排序后的 `(seq0, seq1, seq2, "chi")`。  
- 吃后：`current_player = responder`，`has_drawn_wall_this_turn = True`，**不额外摸牌**（与碰类似，当前实现语义为：吃完进入自己出牌阶段）。

### 9.4 荣胡执行（`_execute_rong_hu`）

- 从河中移除对应 `(D, tile)`。  
- `current_player = winner`；`has_drawn_wall_this_turn = True`；`phase = "turn"`。  
- **注意**：实现 **不把胡牌手牌结构改变为“亮倒”**，仅在分数字符串里展示；河牌该张被移除。

---

## 10. 胡牌（自摸 / 荣胡）

### 10.1 胡牌判定核心：`evaluate_hand(hand, melds)`

- 将 **手牌 + 明面** 合并为 multiset（`_virtual_counter_for_win`），再判断是否存在 **标准形**：  
  > **1 对将 + 其余全部为顺子/刻子（万筒条）或杂牌刻子**  

#### 10.1.1 合并 multiset 的规则

- **吃**：三张点数牌各 +1。  
- **碰**：`tile` +3。  
- **拢**：`tile` +4（四张一拢）。  
- **磕** `(t, None)`：`t` +3。  
- **磕** `(t, sub)`：`t` +2，`sub` +1（`sub` 为督）。  

#### 10.1.2 将牌（雀头）枚举 `_try_remove_pair`

依次尝试移除“一对将”后，剩余牌能否全部组成面子（见下）。将牌可为：

- 任意牌 `t` 数量 ≥ 2：移除两张 `t`。  
- **万督 + 某张万子**：各移除 1，合成一对将。  
- **筒督 + 某张筒子**：同上。  
- **条督 + 某张条子**：同上。  
- **妖督 + 乌龟/毛/千万之一**：各移除 1。  
- **两张同名花色督**（万万/筒筒/条条/妖妖）直接移除两张。  
- **总督一对**：移除两张总督。  
- **总督 + 任意其它一张牌**：各移除 1（总督作万能将另一半）。

#### 10.1.3 面子分解（剩余牌无将）

记剩余牌总数必须 **能被 3 整除**（`_rest_all_melds_no_pair`）。

先把计数拆到：

- 万/筒/条 各 9 维数组；  
- 杂牌三类计数；  
- `万督/筒督/条督/妖督` 的张数；  
- `总督` 的张数。

**无总督时**：要求

- 万子维度 + 万督作为 **万子花色的百搭** 可参与组成 **顺子或刻子**（`_suit_only_melds`）；  
- 筒子 + 筒督；  
- 条子 + 条督；  
- 杂牌仅允许 **刻子（三张相同杂牌）**，妖督作为 **杂牌百搭**（`_misc_only_melds`）。

**有总督时**：将 `总督` 的张数 `u_gov` **拆成四份非负整数** `(uw, uto, uti, um)`，满足和为 `u_gov`，分别加到万/筒/条/杂的百搭额度上；**任意一种拆分**能使四类子问题全为真，则整体为真（枚举 `_iter_nonneg_splits4`）。

`_suit_only_melds(arr9, jokers)`：深度优先，能组 **顺子**（连续三点，不足用 jokers 顶）或 **刻子**（同点三张，不足用 jokers 顶）。

`_misc_only_melds`：三个杂牌位只能用 **刻子** 消化；妖督数量 `yao_wild` 可用于补齐杂刻。

#### 10.1.4 基础分

- `evaluate_hand` 返回：若可胡则 `score = 100`，否则 0（**与牌型番种无关，固定 100**）。

### 10.2 自摸胡（POST `action=hu`）

- 条件：`phase=="turn"`、轮到本人、已摸牌、`evaluate_hand` 为真。  
- 展示分：`total_score = base_score(100) + meld_score`，其中对 **每一组明面** 循环累加：

| 明面类型 | `meld_score` |
|----------|----------------|
| 拢：`len==3` 且 `meld[2]=="long"` | +70 |
| 碰：`len==3` 且 `meld[2]=="pong"` | +10 |
| **其它全部**（含：吃 `len==4`、磕 `len==2` 等） | +20 |

注意：这与询价荣胡展示里使用的 `_meld_score_sum` **口径不同**（那里规定吃 +0、其它非拢非碰 +20）。当前代码存在两套计分展示逻辑。

### 10.3 荣胡（询价阶段 `action=rong_hu`）

- 使用 `evaluate_hand(player_hand + [tile_c], player_melds)`；不可胡则 400。  
- 执行 `_execute_rong_hu`。  
- 展示：`总分 = result['score'](100) + _meld_score_sum(player_melds)`，其中 `_meld_score_sum` 规定：**吃 +0**，拢 +70，碰 +10，其它 +20。

---

## 11. 磕牌（POST 带 `meld_tile`，可选 `substitute`）

- 须在 `phase=="turn"`、轮到本人、已摸牌，且无未完成询价。  
- **无刻督**：手牌中 `meld_tile` **≥ 3** 张 → 移除三张，明面 `(meld_tile, None)`。  
- **带督**：`substitute` 在手牌中；且  
  - 若 `substitute == "妖督"`，则 `meld_tile` 必须是 **乌龟/毛/千万之一**；  
  - 且手牌中 `meld_tile` **≥ 2** 张；  
  → 移除两张 `meld_tile` + 一张 `substitute`，明面 `(meld_tile, substitute)`。  
- **磕不立即改变 `scores`**（与碰不同）。

---

## 12. 拢牌（POST `action=long`，参数 `long_tile`）

- 须在 `phase=="turn"`、轮到本人、已摸牌，且无询价。  

### 12.1 补拢（已存在磕明面 `(tile, None)` 或 `(tile, substitute)` 且尚未标记 `"long"`）

- 找到第一个满足：`len(meld)>=2`、`meld[0]==long_tile`、`meld[1] is None`、`"long" not in meld` 的磕（即 **无刻督的磕** 才会被此分支匹配；带督的磕 `meld[1]` 非 None，**不会被补拢分支匹配**——实现现状）。  
- 玩家可用手牌中 **再一张同点 `tile`**，或 **对应花色督**（`六万→万督`，`三筒→筒督`，`九条→条督`）完成拢：  
  - 移除用于补的那张牌；  
  - 将该明面改为 `(tile, None, "long")`；  
  - **`scores[玩家] += 70`**；  
  - 若牌墙非空：**再摸一张**；更新 `draw_pile`。  

### 12.2 直接拢（手牌中同一 `tile` **≥ 4** 张）

- 移除四张，追加明面 `(tile, None, "long")`；**+70**；再摸一张（若牌墙有牌）。

---

## 13. 与人机对局的推进（GET `/play`）

- 每次渲染前调用 `_advance_ai_until_human_turn`：  
  - 先尽量清空 AI 询价队列直到轮到真人；  
  - 若当前行牌者非真人：只要 **还没摸牌** 就摸一张；若已摸牌就 **随机打出一张手牌** 并 `_finish_discard`。  
- 若牌墙空：重新生成整副牌。

---

## 14. 排序与显示（与胜负无关）

- 手牌排序键：万 → 筒 → 条 → 杂（乌龟、毛、千万）→ 督（万督、筒督、条督、妖督、总督）；同类按点数或固定次序。

---

## 15. 当前实现与常见实体麻将的差异（便于你后续改规则）

- **荒牌流局**：无；牌墙会无限重生。  
- **一炮多响 / 多家荣胡**：无；询价是顺序单个响应者。  
- **抢杠、杠牌类型**：无杠动作；拢为明面四张，不等同“杠包牌”。  
- **吃碰优先级**：人机用随机；真人端需自行点选；无全局规则裁决。  
- **督牌**：打出后不可被吃碰胡（仅实现层面禁止询价）；督在手牌内仍按胡牌算法可当百搭（视种类）。  
- **分数**：多种口径并存（立即 `scores`、胡牌字符串里的 base+meld）；日后应以文档统一。

---

## 16. 修订记录（文档维护）

- **初版**：对应仓库内 `app.py` 行为梳理；包含胡牌 DFS、总督拆分、督打出禁询价、拢补花色督等算法要点。

你在本文档上直接增删改后，可把 **新增条目编号 + 期望的判定函数入口**（例如“应在 `_finish_discard` 或 `evaluate_hand` 增加 XXX”）一并标注，便于下一轮按文档改代码。
